#include<stdio.h>
#include<math.h>
int main()
{
    int a,b;
    scanf("%d",&a);
    if(0<=a && a<=pow(10,9))
    {
        scanf("%d",&b);
        if(0<=b && b<=pow(10,9))
            printf("");
    }




}
